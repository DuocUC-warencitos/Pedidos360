server:
    port: ${SERVER_PORT:8080}

spring:
    application:
        name: pedidos-gateway
    security:
        oauth2:
            resourceserver:
                jwt:
                    issuer-uri: ${JWT_ISSUER_URI}
    cloud:
        gateway:
            server:
                webflux:
                    routes: