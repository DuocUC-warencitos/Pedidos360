server:
    port: ${SERVER_PORT:8081}

spring:
    datasource:
        url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-0-us-east-1.pooler.supabase.com:6543/postgres?ssl=true&sslmode=require&pgbouncer=true&prepareThreshold=0}
        username: ${SPRING_DATASOURCE_USERNAME:postgres.myunbjgdvtkkvgaqemwz}
        password: ${SPRING_DATASOURCE_PASSWORD:oeyA3CqStCq0LwHx}
        driver-class-name: org.postgresql.Driver
    application:
        name: pedidos-service
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: false
        open-in-view: false
        database-platform: org.hibernate.dialect.PostgreSQLDialect
    security:
        oauth2:
            resourceserver:
                jwt:
                    issuer-uri: ${JWT_ISSUER_URI:https://login.microsoftonline.com/1f3a849e-c198-4ff7-b67c-d17f15cbc152/v2.0}
    cloud:
        openfeign:
            client:
                config:
                    productoService:
                        connect-timeout: 2000
                        read-timeout: 3000
                        logger-level: basic